================================================================================
  TOPSHELF INVENTORY — MOBILE API REFERENCE
  Base URL: https://www.topshelfInventory.com
  All data is scoped to the authenticated user's organization.
================================================================================


────────────────────────────────────────────────────────────────────────────────
OVERVIEW
────────────────────────────────────────────────────────────────────────────────

All mobile API endpoints live under /api/mobile/.

Authentication uses JWT Bearer tokens — not cookies.
  1. Call POST /api/mobile/auth to get a token.
  2. Include the token in every subsequent request:
       Authorization: Bearer <your-token>

Tokens expire after 7 days. Store them securely:
  iOS    → Keychain (via react-native-keychain or expo-secure-store)
  Android → EncryptedSharedPreferences or Keystore

Every response is JSON. Every request body must be JSON with the header:
  Content-Type: application/json

All data returned is filtered to the authenticated user's organization —
no cross-organization data leakage is possible.


────────────────────────────────────────────────────────────────────────────────
1. AUTHENTICATION — Login and get a token
────────────────────────────────────────────────────────────────────────────────

POST /api/mobile/auth

No Authorization header needed for this request.

REQUEST BODY:
{
  "organization_subdomain": "your-bar",     ← required (your org's subdomain)
  "email": "admin@yourbar.com",             ← use EITHER email+password
  "password": "yourpassword"
}

OR:
{
  "organization_subdomain": "your-bar",
  "pin": "1234"                             ← OR a 4-digit PIN
}

RESPONSE (200 OK):
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "expires_in": 604800,
  "user": {
    "id": 42,
    "first_name": "Alex",
    "last_name": "Smith",
    "email": "admin@yourbar.com",
    "role": "admin",
    "permissions": ["all"]
  },
  "organization": {
    "id": 7,
    "name": "The Rusty Nail",
    "subdomain": "your-bar",
    "subscription_plan": "pro"
  }
}

ERROR RESPONSES:
  401 { "error": "Invalid credentials" }
  401 { "error": "Invalid PIN" }
  404 { "error": "Organization not found" }
  400 { "error": "Provide email+password or pin" }

USAGE AFTER LOGIN:
  Save the "token" value and send it on every future request:
    Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...


────────────────────────────────────────────────────────────────────────────────
2. REGISTRATION — Join via admin invite link
────────────────────────────────────────────────────────────────────────────────

Admins generate invite links from Settings → Users → "Invite New User by Email."
The link looks like:
  https://www.topshelfInventory.com/register/invite?token=abc123...

The token in that URL is what the mobile app uses. No Authorization header
is needed for either registration endpoint.

── Step 1: Validate the token ──────────────────────────────────────────────────

GET /api/mobile/register?token=<invite-token>

RESPONSE (200 OK):
{
  "valid": true,
  "org_name": "The Rusty Nail",
  "email": "newstaff@yourbar.com",   ← pre-filled email (may be null)
  "role": "user"
}

ERROR RESPONSES:
  404 { "error": "Invalid token" }
  410 { "error": "Invitation already used" }
  410 { "error": "Invitation expired" }

── Step 2: Complete registration ────────────────────────────────────────────────

POST /api/mobile/register

REQUEST BODY:
{
  "token": "abc123...",              ← required — from the invite link
  "firstName": "Alex",              ← required
  "lastName": "Smith",              ← required
  "displayName": "Alex S.",         ← optional (shown in feed & messages)
  "email": "alex@yourbar.com",      ← required if not pre-filled by admin
  "phone": "5550001234",            ← optional
  "password": "mysecurepassword",   ← required, min 6 characters
  "pin": "1234"                     ← required, exactly 4 digits
}

RESPONSE (200 OK):
{
  "token": "eyJhbGciOiJIUzI1NiIs...",   ← save this, use as Bearer token
  "expires_in": 604800,
  "user": {
    "id": 55,
    "first_name": "Alex",
    "last_name": "Smith",
    "role": "user",
    "email": "alex@yourbar.com"
  },
  "organization": {
    "id": 7,
    "name": "The Rusty Nail"
  }
}

ERROR RESPONSES:
  400 { "error": "First and last name are required" }
  400 { "error": "Password must be at least 6 characters" }
  400 { "error": "PIN must be exactly 4 digits" }
  409 { "error": "Email already in use" }
  410 { "error": "Invitation already used" }
  410 { "error": "Invitation expired" }

NOTE: Each invite token can only be used once. After successful registration,
use the returned "token" exactly like any other Bearer token for all subsequent
API calls. The user is automatically assigned to all locations in the org.


────────────────────────────────────────────────────────────────────────────────
3. DEVICE TOKENS — Push notification registration
────────────────────────────────────────────────────────────────────────────────

Call these after login and whenever the FCM/APNs token refreshes.
Push notifications require Firebase FCM to be configured on the server.
If not configured, push is silently disabled but all other API calls work normally.

POST /api/mobile/device-token

REQUEST BODY:
{
  "token": "fcm-or-apns-token-string",
  "platform": "ios"    ← or "android"
}

RESPONSE (200 OK):
  { "ok": true }

── Unregister on logout ─────────────────────────────────────────────────────────

DELETE /api/mobile/device-token

REQUEST BODY:
  { "token": "specific-token" }    ← omit to remove ALL tokens for this user

RESPONSE (200 OK):
  { "ok": true }


────────────────────────────────────────────────────────────────────────────────
4. NOTIFICATIONS — In-app notification inbox
────────────────────────────────────────────────────────────────────────────────

GET /api/mobile/notifications

QUERY PARAMETERS (all optional):
  ?unread_only=true    Only return unread notifications
  ?limit=30            Max to return (default: 30, max: 100)

RESPONSE (200 OK):
{
  "notifications": [
    {
      "id": 10,
      "type": "swap_request",
      "title": "🔄 Shift Swap Request",
      "message": "Alex Smith wants to swap shifts with you on 2026-05-10",
      "data": { "swap_id": "42", "type": "swap_request" },
      "is_read": false,
      "created_at": "2026-05-04T18:00:00Z"
    }
  ],
  "unread_count": 3
}

── Mark as read ─────────────────────────────────────────────────────────────────

PATCH /api/mobile/notifications

REQUEST BODY:
  { "id": 10 }    ← omit id to mark ALL notifications as read

RESPONSE (200 OK):
  { "ok": true }


────────────────────────────────────────────────────────────────────────────────
5. DASHBOARD FEED — Organization activity posts
────────────────────────────────────────────────────────────────────────────────

GET /api/mobile/feed

QUERY PARAMETERS (all optional):
  ?page=1        Page number (default: 1)
  ?limit=20      Results per page (max: 50, default: 20)

RESPONSE (200 OK):
{
  "posts": [
    {
      "id": 101,
      "content": "Counted the liquor shelf — all good!",
      "images": ["data:image/jpeg;base64,/9j/4AAQ..."],
      "tagged_user_ids": [3, 8],
      "created_at": "2026-04-30T14:22:00Z",
      "user_id": 5,
      "author_name": "Alex Smith",
      "author_avatar": "data:image/jpeg;base64,...",
      "like_count": 4,
      "comment_count": 2,
      "liked_by_me": false
    }
  ],
  "total": 84,
  "page": 1,
  "users": [
    { "id": 3, "display_name": "Jamie Lee", "profile_picture": null },
    { "id": 8, "display_name": "Morgan D.", "profile_picture": "data:image/jpeg;base64,..." }
  ]
}

NOTES:
- "images" is an array of base64 data-URL strings. Decode and display directly.
- "tagged_user_ids" references IDs in the "users" array returned alongside posts.
- "like_count" / "comment_count" / "liked_by_me" are pre-computed for the feed.
- Posts are returned newest-first. Paginate with ?page=2, ?page=3, etc.

── Create a post ────────────────────────────────────────────────────────────────

POST /api/mobile/feed

REQUEST BODY:
{
  "content": "Just finished the audit!",   ← at least one of content or images required
  "images": ["data:image/jpeg;base64,..."],
  "tagged_user_ids": [3, 8]
}

RESPONSE (200 OK):
  { "ok": true, "id": 102, "created_at": "2026-05-04T19:00:00Z" }

── Like / Unlike a post ─────────────────────────────────────────────────────────

POST /api/mobile/feed/like?id=<postId>

No request body needed.

RESPONSE (200 OK):
  { "liked": true, "count": 5 }   ← "liked" is the new state after toggle

── Comments ─────────────────────────────────────────────────────────────────────

GET /api/mobile/feed/comments?postId=<id>

RESPONSE (200 OK):
{
  "comments": [
    {
      "id": 7,
      "content": "Great work!",
      "created_at": "2026-05-04T19:10:00Z",
      "user_id": 3,
      "author_name": "Jamie Lee",
      "author_avatar": null
    }
  ]
}

POST /api/mobile/feed/comments?postId=<id>

REQUEST BODY:
  { "content": "Looks good!" }

RESPONSE (200 OK):
{
  "ok": true,
  "comment": {
    "id": 8,
    "content": "Looks good!",
    "created_at": "2026-05-04T19:15:00Z",
    "user_id": 42,
    "author_name": "Alex Smith",
    "author_avatar": null
  }
}

DELETE /api/mobile/feed/comments?id=<commentId>

Can only delete your own comments. Admins can delete any comment.

RESPONSE (200 OK):
  { "ok": true }


────────────────────────────────────────────────────────────────────────────────
6. BARRED LIST — Persons barred from the venue
────────────────────────────────────────────────────────────────────────────────

GET /api/mobile/barred

QUERY PARAMETERS (all optional):
  ?trespassed_only=true    Only return persons with a formal trespass order

RESPONSE (200 OK):
{
  "barred": [
    {
      "id": 1,
      "name": "John Doe",
      "aliases": ["Johnny D", "JD"],
      "photo": "data:image/jpeg;base64,...",
      "description": "Involved in assault incident on 2026-03-12.",
      "barred_by_name": "Alex Smith",
      "trespassed": true,
      "barred_until": null,
      "created_at": "2026-03-12T20:00:00Z"
    }
  ],
  "total": 1
}

NOTE: "barred_until" is null for permanent bans. Expired bans are auto-archived
and will not appear in this list.

⚠ If "trespassed" is true, display a prominent warning — this person has a
  formal trespass order and must not be admitted under any circumstances.

── Add a barred person from mobile ──────────────────────────────────────────────

POST /api/mobile/barred

Requires admin role OR the 'add_barred' permission.

REQUEST BODY:
{
  "name": "John Doe",               ← required
  "aliases": ["Johnny D"],          ← optional
  "photo": "data:image/jpeg;...",   ← optional, base64 image (max 50MB)
  "description": "Details...",      ← optional
  "trespassed": true,               ← optional, default false
  "barred_until": "2026-08-01T00:00:00Z"  ← optional, null = permanent
}

RESPONSE (200 OK):
  { "barred": { ...full barred person row } }


────────────────────────────────────────────────────────────────────────────────
7. OUT-OF-STOCK / LOW STOCK ITEMS
────────────────────────────────────────────────────────────────────────────────

GET /api/mobile/out-of-stock

QUERY PARAMETERS (all optional):
  ?location_id=2    Filter to a specific location ID

RESPONSE (200 OK):
{
  "items": [
    {
      "id": 88,
      "name": "Tito's Vodka 750ml",
      "type": "Liquor",
      "secondary_type": "Vodka",
      "quantity": 0,
      "low_stock_threshold": 3,
      "order_size": [{ "label": "Case", "amount": 12 }],
      "supplier": "Southern Glazers"
    }
  ],
  "total": 12,
  "out_of_stock": 4,
  "low_stock": 8
}

NOTES:
- "out_of_stock" = items with quantity exactly 0
- "low_stock" = items with quantity > 0 but <= low_stock_threshold
- Items sorted by quantity ascending (most urgent first)


────────────────────────────────────────────────────────────────────────────────
8. SCHEDULE — View the org schedule
────────────────────────────────────────────────────────────────────────────────

GET /api/mobile/schedule

All results are automatically scoped to the authenticated user's organization.
You cannot retrieve another organization's schedule.

QUERY PARAMETERS (all optional):
  ?view=week|month         View mode hint (default: week — affects default range)
  ?start=YYYY-MM-DD        Range start date (inclusive)
  ?end=YYYY-MM-DD          Range end date (inclusive, defaults to start if omitted)
  ?location_id=<id>        Filter to a specific location ID

SCOPE: Every response only returns users who belong to the authenticated user's
organization. The "is_mine" flag marks shifts belonging to the current user.

── REQUEST VARIATIONS ───────────────────────────────────────────────────────────

Single day (today's schedule):
  GET /api/mobile/schedule?start=2026-05-04&end=2026-05-04

Single day (any specific date):
  GET /api/mobile/schedule?start=2026-05-10&end=2026-05-10

Single day filtered by location:
  GET /api/mobile/schedule?start=2026-05-04&end=2026-05-04&location_id=2

Current week (Mon–Sun, auto-computed by server):
  GET /api/mobile/schedule
  GET /api/mobile/schedule?view=week

Explicit week range:
  GET /api/mobile/schedule?start=2026-05-04&end=2026-05-10
  GET /api/mobile/schedule?view=week&start=2026-05-04&end=2026-05-10

Week filtered by location:
  GET /api/mobile/schedule?start=2026-05-04&end=2026-05-10&location_id=2

Current month (auto-computed by server):
  GET /api/mobile/schedule?view=month

Explicit month range:
  GET /api/mobile/schedule?start=2026-05-01&end=2026-05-31
  GET /api/mobile/schedule?view=month&start=2026-05-01&end=2026-05-31

Month filtered by location:
  GET /api/mobile/schedule?view=month&start=2026-05-01&end=2026-05-31&location_id=2

Multi-week or arbitrary range (any start/end you need):
  GET /api/mobile/schedule?start=2026-05-01&end=2026-05-21

Note: The server internally fetches one additional day before "start" to capture
overnight shifts that spill into the range start date. Those entries have
"is_pre_range": true so the client can filter them from display if needed.

── RESPONSE (200 OK) ────────────────────────────────────────────────────────────

{
  "schedules": [
    {
      "id": 301,
      "date": "2026-05-05",           ← always YYYY-MM-DD (local date, no timezone shift)
      "user_id": 42,
      "shift_id": 5,
      "recurring_group_id": null,
      "shift_name": "Evening",
      "start_time": "17:00",          ← HH:MM 24-hour format
      "end_time": "23:00",
      "color": "#1d4ed8",
      "user_name": "Alex Smith",
      "user_avatar": null,            ← base64 data-URL or null
      "position": "Head Bartender",
      "is_mine": true,                ← true only for the authenticated user's shifts
      "crosses_midnight": false,      ← true when start_time > end_time (overnight shift)
      "spillover_date": null,         ← YYYY-MM-DD of next day if crosses_midnight, else null
      "is_pre_range": false           ← true if this entry was from the day before requested start
    },
    {
      "id": 302,
      "date": "2026-05-05",
      "shift_name": "Closing",
      "start_time": "22:00",
      "end_time": "02:00",            ← end < start = overnight shift
      "crosses_midnight": true,
      "spillover_date": "2026-05-06", ← this shift continues into May 6
      "is_pre_range": false,
      ...
    }
  ],
  "pending_swaps": [ ...swap objects (see Shift Swaps section) ],
  "start": "2026-05-05",             ← the start date you requested
  "end": "2026-05-11",               ← the end date used
  "view": "week"
}

── OVERNIGHT SHIFTS ─────────────────────────────────────────────────────────────

When "crosses_midnight" is true, the shift starts on "date" and ends on
"spillover_date". The end_time is on the following calendar day.

To show overnight continuation in a day view:
  1. Display the shift normally on its "date" (show start_time, note "+1" on end_time)
  2. On "spillover_date", show a continuation entry using the same record:
       filter: schedules.filter(s => s.crosses_midnight && s.spillover_date === targetDate)
       display: "until <end_time>" with an "↩ overnight" badge

To check if a day has overnight continuation from the previous day:
  spillovers = schedules.filter(s => s.crosses_midnight && s.spillover_date === dateString)

── FILTERING ONLY YOUR OWN SHIFTS (client-side) ─────────────────────────────────

All schedules are returned for the org. To show only the authenticated user's shifts:
  myShifts = schedules.filter(s => s.is_mine === true)

── NOTES ────────────────────────────────────────────────────────────────────────

- All schedules are scoped to the authenticated user's organization automatically.
- "is_mine" flags shifts belonging to the authenticated user only.
- "pending_swaps" lists open swap requests involving the user — no extra call needed.
- Archived users are excluded from results.
- "color" comes from the shift definition. Use it for color-coding schedule blocks.
- Dates are always raw YYYY-MM-DD strings — compare as strings, not Date objects,
  to avoid timezone shift bugs (do not use new Date(s.date).getDate()).
- The "location_id" filter uses JOIN — users without a location assignment are
  excluded when this filter is active.


────────────────────────────────────────────────────────────────────────────────
9. SHIFT SWAPS — Request and manage shift swaps
────────────────────────────────────────────────────────────────────────────────

There are two types of swap requests:

  DIRECT — Requester selects a specific colleague and their shift to swap with.
  OPEN   — Requester posts their shift openly to all org members. Anyone can
           claim it. Supports both "swap" (two-way) and "give away" (one-way).

Status machines:

  Direct swap:
    pending_employee → (target accepts) → pending_manager → (manager approves) → approved
                     → (target declines) → declined
                                         → (manager declines) → declined

  Open request (swap or give away):
    open → (any employee claims via PUT) → pending_manager → (manager approves) → approved
                                                           → (manager declines) → declined

── List swap requests ───────────────────────────────────────────────────────────

GET /api/mobile/schedule/swap

Returns swap requests you're involved in (as requester or target), plus any
open org-wide requests currently in "open" status (claimable by you).

QUERY PARAMETERS (all optional):
  ?status=open|pending_employee|pending_manager|approved|declined

RESPONSE (200 OK):
{
  "swaps": [
    {
      "id": 10,
      "status": "pending_manager",
      "request_type": "direct",      ← "direct" | "open"
      "is_giveaway": false,           ← true = one-way giveaway, false = mutual swap
      "message": "Can we swap? I have a family thing.",
      "decline_reason": null,
      "requester_id": 42,
      "target_id": 55,               ← null for unclaimed open requests
      "employee_responded_at": "2026-05-04T20:00:00Z",
      "manager_responded_at": null,
      "created_at": "2026-05-04T18:00:00Z",
      "requester_name": "Alex Smith",
      "requester_avatar": null,
      "target_name": "Jamie Lee",    ← null for unclaimed open requests
      "target_avatar": null,
      "requester_date": "2026-05-10",
      "requester_shift": "Evening",
      "requester_start": "17:00",
      "requester_end": "23:00",
      "target_date": "2026-05-11",   ← null for giveaway or unclaimed open requests
      "target_shift": "Morning",
      "target_start": "09:00",
      "target_end": "15:00",
      "status_detail": "Jamie Lee accepted! Waiting for a manager to approve."
    }
  ]
}

status_detail strings (computed server-side, vary by who is viewing):

  Direct swap:
    "Jane wants to swap shifts with you. Waiting for your response."
    "Waiting for Jane to accept or decline your swap request."
    "Jane accepted! Waiting for a manager to approve."
    "You accepted. Waiting for a manager to approve the swap."
    "Swap approved! Alice and Bob have swapped shifts."
    "Swap declined: Manager was unable to accommodate."

  Open request (requester view):
    "You've posted your Evening shift on 2026-05-10 for anyone to take."
    "You've posted your Evening shift on 2026-05-10 as open for anyone to swap with."
    "Jamie Lee claimed your open shift request. Waiting for a manager to approve."

  Open request (other employees view):
    "Alex Smith is giving away their Evening shift on 2026-05-10. Tap to claim it."
    "Alex Smith is looking for someone to swap their Evening shift on 2026-05-10."

── Create a swap request ────────────────────────────────────────────────────────

POST /api/mobile/schedule/swap

DIRECT SWAP — target a specific colleague:
{
  "my_schedule_id": 301,       ← your user_schedules.id  (required)
  "their_schedule_id": 402,    ← colleague's user_schedules.id  (required for direct)
  "target_user_id": 55,        ← colleague's user id  (required for direct)
  "message": "Can we swap?"    ← optional note
}

OPEN REQUEST — broadcast to all org members:
{
  "my_schedule_id": 301,       ← your user_schedules.id  (required)
  "is_giveaway": true,         ← true = give away shift, false/omit = open swap
  "message": "Anyone free?"    ← optional note
}
Omit their_schedule_id and target_user_id entirely to create an open request.

RESPONSE (200 OK):
  { "ok": true, "swap_id": 10 }

Side effects:
  Direct: push notification sent to target employee.
  Open:   push notification broadcast to all org members.

ERROR RESPONSES:
  400 { "error": "my_schedule_id is required" }
  404 { "error": "Your schedule entry not found" }
  404 { "error": "Target schedule entry not found" }
  409 { "error": "A pending swap request already exists for that shift" }

── Claim an open request ────────────────────────────────────────────────────────

PUT /api/mobile/schedule/swap

Any org member can claim an open request. For open swaps (is_giveaway=false),
provide your schedule entry. For give-aways, no schedule entry is needed —
you're simply agreeing to cover the shift.

REQUEST BODY:
{
  "swap_id": 10,               ← required
  "my_schedule_id": 205        ← required for open swaps, omit for give-aways
}

RESPONSE (200 OK):
  { "ok": true }

Side effects: notifies the requester that their shift was claimed + notifies
all admins that manager approval is needed. Status moves to pending_manager.

ERROR RESPONSES:
  400 { "error": "swap_id is required" }
  400 { "error": "This is not an open request" }
  400 { "error": "You cannot claim your own request" }
  400 { "error": "my_schedule_id is required to claim a swap request" }
  404 { "error": "Swap request not found" }
  404 { "error": "Your schedule entry not found" }
  409 { "error": "This request is no longer open" }

── Respond to a direct swap request (target employee only) ──────────────────────

PATCH /api/mobile/schedule/swap

Only the designated target employee of a direct swap uses this endpoint.
Open requests use PUT (claim) above instead.

REQUEST BODY:
{
  "swap_id": 10,
  "action": "accept",           ← or "decline"
  "decline_reason": "..."       ← optional, only used when action = "decline"
}

RESPONSE (200 OK):
  { "ok": true }

Side effects:
  - accept: status → pending_manager; notifies requester + all admins
  - decline: status → declined; notifies requester with decline message


────────────────────────────────────────────────────────────────────────────────
10. TIME-OFF REQUESTS
────────────────────────────────────────────────────────────────────────────────

── List my time-off requests ────────────────────────────────────────────────────

GET /api/mobile/schedule/time-off

QUERY PARAMETERS (all optional):
  ?status=pending|approved|declined

RESPONSE (200 OK):
{
  "requests": [
    {
      "id": 5,
      "start_date": "2026-06-01",
      "end_date": "2026-06-03",
      "reason": "Family vacation",
      "status": "pending",
      "created_at": "2026-05-04T18:00:00Z",
      "reviewed_at": null,
      "reviewed_by_name": null
    }
  ]
}

── Submit a time-off request ────────────────────────────────────────────────────

POST /api/mobile/schedule/time-off

REQUEST BODY:
{
  "start_date": "2026-06-01",   ← YYYY-MM-DD, required
  "end_date": "2026-06-03",     ← YYYY-MM-DD, required (>= start_date)
  "reason": "Family vacation"   ← optional
}

RESPONSE (200 OK):
  { "ok": true, "id": 5 }

Side effect: push notification sent to all admins.

── Approve or decline a request (admin only) ────────────────────────────────────

PATCH /api/mobile/schedule/time-off

REQUEST BODY:
{
  "id": 5,
  "action": "approve",          ← or "decline"
  "decline_reason": "..."       ← optional, only used when action = "decline"
}

RESPONSE (200 OK):
  { "ok": true }

Side effect: push notification sent to the requesting employee.

ERROR RESPONSES:
  403 { "error": "Admin access required" }
  404 { "error": "Request not found" }
  409 { "error": "Request has already been reviewed" }


────────────────────────────────────────────────────────────────────────────────
11. MESSAGING — Direct and group messages between employees
────────────────────────────────────────────────────────────────────────────────

Messaging uses a thread model. A thread is either "direct" (1-on-1) or "group"
(2+ other recipients). You always appear in the thread as a member.

── DB MIGRATION REQUIRED ────────────────────────────────────────────────────────

Before using messaging, run the migration on the server:
  psql -U postgres topshelf -f scripts/mobile-features-migration.sql

This creates: message_threads, message_thread_members, direct_messages,
              mobile_user_settings

── List threads (inbox) ─────────────────────────────────────────────────────────

GET /api/mobile/messages

RESPONSE (200 OK):
{
  "threads": [
    {
      "id": 1,
      "type": "direct",                          ← "direct" or "group"
      "name": null,                              ← group name (null for direct)
      "updated_at": "2026-05-05T12:00:00Z",
      "members": [
        {
          "id": 55,
          "name": "Jamie Lee",
          "avatar": "data:image/jpeg;base64,...",
          "position": "Bartender"
        }
      ],
      "last_message": {
        "id": 200,
        "content": "See you tonight!",
        "sender_id": 55,
        "created_at": "2026-05-05T11:58:00Z"
      },
      "unread_count": 2
    }
  ]
}

NOTES:
- "members" excludes yourself — it is the other people in the thread.
- "unread_count" counts messages sent by others after your last_read_at timestamp.
- Threads are sorted newest-updated first.
- For direct threads, derive the thread display name from members[0].name.

── Start a new thread or send an initial message ────────────────────────────────

POST /api/mobile/messages

REQUEST BODY:
{
  "user_ids": [55],                   ← required: who to message (not yourself)
  "message": "Hey, are you working tonight?",  ← at least one of message/image required
  "image": "data:image/jpeg;base64,...",       ← optional, base64 data-URI
  "thread_name": "Closing Crew"       ← optional, only used for group threads (user_ids.length > 1)
}

BEHAVIOR:
- user_ids.length === 1 → direct thread. If a direct thread already exists between
  you and that user, the message is added to it (no duplicate threads).
- user_ids.length >= 2  → group thread. Always creates a new thread.
- You do not need to include yourself in user_ids — the server adds you automatically.
- All user_ids must belong to your organization; mismatches return 400.

RESPONSE (200 OK):
{
  "ok": true,
  "thread_id": 1,
  "message_id": 200,
  "created_at": "2026-05-05T12:00:00Z"
}

Side effect: FCM push notification sent to all recipients.

ERROR RESPONSES:
  400 { "error": "user_ids required" }
  400 { "error": "message or image required" }
  400 { "error": "image must be a base64 data-URI" }
  400 { "error": "One or more users not found in your organization" }

── Get messages in a thread (paginated) ─────────────────────────────────────────

GET /api/mobile/messages/<threadId>

QUERY PARAMETERS (all optional):
  ?limit=50        Max messages to return (default: 50, max: 100)
  ?before=<ISO>    Cursor for pagination: return messages older than this timestamp

Automatically marks the thread as read for your user.

RESPONSE (200 OK):
{
  "thread": {
    "id": 1,
    "type": "direct",
    "name": null,
    "members": [ { "id": 55, "name": "Jamie Lee", "avatar": null, "position": "Bartender" } ]
  },
  "messages": [
    {
      "id": 198,
      "sender_id": 55,
      "content": "Hey!",
      "image": null,
      "created_at": "2026-05-05T11:55:00Z",
      "sender_name": "Jamie Lee",
      "sender_avatar": null
    },
    {
      "id": 199,
      "sender_id": 42,
      "content": "What's up?",
      "image": null,
      "created_at": "2026-05-05T11:57:00Z",
      "sender_name": "Alex Smith",
      "sender_avatar": null
    }
  ],
  "has_more": false    ← true if there are older messages; paginate with ?before=<oldest created_at>
}

NOTES:
- Messages are returned oldest-first (ascending created_at).
- For pagination: take the oldest message's created_at and pass it as ?before=<ISO>.
- "image" is a base64 data-URI string or null.
- To show images: if message.image is not null, render it as an <Image> instead of text.

── Send a message to an existing thread ─────────────────────────────────────────

POST /api/mobile/messages/<threadId>

REQUEST BODY:
{
  "message": "On my way!",                       ← at least one of message/image required
  "image":   "data:image/jpeg;base64,..."        ← optional
}

RESPONSE (200 OK):
{
  "ok": true,
  "message_id": 201,
  "created_at": "2026-05-05T12:10:00Z"
}

Side effect: FCM push sent to all thread members except you.

── Leave a thread ────────────────────────────────────────────────────────────────

DELETE /api/mobile/messages/<threadId>

Removes you from the thread's member list. The thread and its messages are preserved
for the remaining members. You will no longer receive push notifications for it.

RESPONSE (200 OK):
  { "ok": true }

── Live messaging — polling for new messages ─────────────────────────────────────

GET /api/mobile/messages/poll?since=<ISO>

Returns all new messages across ALL your threads since the given timestamp.
Also returns updated unread counts for affected threads.

Use this to keep the inbox live while the app is in the foreground.
Recommended interval: every 5–8 seconds while the messages screen is open.

For background updates: rely on FCM push (type: "new_message"). When a push
arrives, call GET /api/mobile/messages/poll?since=<last_poll_time> to refresh.

REQUEST:
  GET /api/mobile/messages/poll?since=2026-05-05T12:00:00.000Z

RESPONSE (200 OK):
{
  "messages": [
    {
      "id": 202,
      "thread_id": 1,
      "sender_id": 55,
      "content": "Are you there?",
      "image": null,
      "created_at": "2026-05-05T12:02:00Z",
      "sender_name": "Jamie Lee",
      "sender_avatar": null
    }
  ],
  "unread": [
    { "thread_id": 1, "unread_count": "1" }
  ],
  "server_time": "2026-05-05T12:02:05.123Z"   ← use this as your next "since" value
}

POLLING IMPLEMENTATION PATTERN:
  let lastPollTime = new Date().toISOString();
  setInterval(async () => {
    const res = await fetch(`/api/mobile/messages/poll?since=${lastPollTime}`, { headers });
    const { messages, unread, server_time } = await res.json();
    lastPollTime = server_time;   // always advance to server_time, not client Date.now()
    if (messages.length > 0) updateUI(messages, unread);
  }, 6000);

ERROR RESPONSES:
  400 { "error": "since param required (ISO timestamp)" }
  400 { "error": "since must be a valid ISO timestamp" }


────────────────────────────────────────────────────────────────────────────────
12. PROFILE — View and update your user profile
────────────────────────────────────────────────────────────────────────────────

── Get your profile ─────────────────────────────────────────────────────────────

GET /api/mobile/profile

Returns the authenticated user's full profile. Password and PIN fields are
NEVER included in any API response.

RESPONSE (200 OK):
{
  "profile": {
    "id": 42,
    "first_name": "Alex",
    "last_name": "Smith",
    "display_name": "Alex S.",           ← used in feed/messages; falls back to full name if null
    "email": "alex@yourbar.com",
    "phone": "5550001234",
    "bio": "Head bartender. Whiskey enthusiast.",
    "role": "user",                      ← "user" or "admin"
    "position": "Head Bartender",
    "profile_picture": null,             ← base64 data-URI or null
    "created_at": "2025-09-01T00:00:00Z",
    "notifications_enabled": true,
    "notify_messages": true,
    "notify_post_likes": true,
    "notify_post_comments": true,
    "notify_schedule": true,
    "notify_swaps": true,
    "notify_time_off": true,
    "address": null,
    "city": null,
    "state": null,
    "zip": null,
    "country": null
  }
}

── Update your profile ───────────────────────────────────────────────────────────

PATCH /api/mobile/profile

Send only the fields you want to change. All fields are optional.
Whitelisted updatable fields: first_name, last_name, display_name, email, phone, bio

REQUEST BODY (example — any subset of these):
{
  "display_name": "Alex S.",
  "phone": "5550009999",
  "bio": "New bio text"
}

To clear a field (set to null): pass null for the field value.

RESPONSE (200 OK):
{
  "ok": true,
  "profile": {
    "id": 42,
    "first_name": "Alex",
    "last_name": "Smith",
    "display_name": "Alex S.",
    "email": "alex@yourbar.com",
    "phone": "5550009999",
    "bio": "New bio text",
    "role": "user",
    "position": "Head Bartender",
    "profile_picture": null,
    "created_at": "2025-09-01T00:00:00Z"
  }
}

ERROR RESPONSES:
  400 { "error": "No valid fields provided" }
  400 { "error": "<field> must be a string or null" }
  409 { "error": "Email already in use" }

── Upload a profile picture ──────────────────────────────────────────────────────

POST /api/mobile/profile/picture

REQUEST BODY:
{
  "image": "data:image/jpeg;base64,/9j/4AAQ..."   ← required, base64 data-URI
}

Accepted MIME types: image/jpeg, image/png, image/webp, image/gif
Max size: 5 MB (encoded, including the data-URI prefix)

The image is stored as a base64 data-URI in the database and returned as
"profile_picture" on GET /api/mobile/profile and in message/feed responses.

RESPONSE (200 OK):
  { "ok": true }

ERROR RESPONSES:
  400 { "error": "image (base64 data-URI) required" }
  400 { "error": "image must be a base64 data-URI (data:image/...)" }
  400 { "error": "Unsupported image type. Allowed: image/jpeg, image/png, image/webp, image/gif" }
  413 { "error": "Image too large (max 5 MB)" }

── Remove profile picture ────────────────────────────────────────────────────────

DELETE /api/mobile/profile/picture

RESPONSE (200 OK):
  { "ok": true }


────────────────────────────────────────────────────────────────────────────────
13. SETTINGS — Notification preferences and address
────────────────────────────────────────────────────────────────────────────────

Settings are stored per-user in the mobile_user_settings table. A default row
is created on first GET (all notifications on, no address).

── Get settings ─────────────────────────────────────────────────────────────────

GET /api/mobile/settings

RESPONSE (200 OK):
{
  "settings": {
    "user_id": 42,
    "notifications_enabled": true,     ← master on/off switch
    "notify_messages": true,           ← direct message push
    "notify_post_likes": true,         ← push when someone likes your post
    "notify_post_comments": true,      ← push when someone comments on your post/thread
    "notify_schedule": true,           ← push for schedule changes
    "notify_swaps": true,              ← push for swap requests/updates
    "notify_time_off": true,           ← push for time-off approval/decline
    "address": null,
    "city": null,
    "state": null,
    "zip": null,
    "country": null,
    "updated_at": "2026-05-05T00:00:00Z"
  }
}

── Update settings ───────────────────────────────────────────────────────────────

PATCH /api/mobile/settings

Send only the fields you want to change. A settings row is upserted if it does
not yet exist.

REQUEST BODY (example — any subset):
{
  "notifications_enabled": true,
  "notify_messages": true,
  "notify_post_likes": false,
  "notify_post_comments": true,
  "notify_schedule": true,
  "notify_swaps": true,
  "notify_time_off": false,
  "address": "123 Main St",
  "city": "New York",
  "state": "NY",
  "zip": "10001",
  "country": "US"
}

Boolean fields must be true/false (not "true"/"false" strings).
String fields accept null to clear.

RESPONSE (200 OK):
{
  "ok": true,
  "settings": { ...updated settings object }
}

ERROR RESPONSES:
  400 { "error": "No valid fields provided" }
  400 { "error": "<field> must be a boolean" }
  400 { "error": "<field> must be a string or null" }

IMPLEMENTATION NOTES FOR MOBILE:
- Check "notifications_enabled" before calling notify(). If false, suppress all
  push. Individual notify_* fields let users opt out of specific push types.
- The server currently sends pushes without checking these preference flags —
  enforce them on the client by not displaying or surfacing certain push types
  when their preference is disabled.
- Store "address" fields here for staff profile display — they are not used by
  the server for any logic.


────────────────────────────────────────────────────────────────────────────────
14. PUSH NOTIFICATION TYPES
────────────────────────────────────────────────────────────────────────────────

Each push notification (and in-app notification) includes a "type" field:

  new_message             — new direct or group message (data: thread_id, sender_id)
  post_liked              — someone liked your post (data: post_id)
  post_comment            — new comment on a post you engaged with (data: post_id)
  swap_request            — new swap request targeting you
  swap_accepted_employee  — your swap request was accepted by the other employee
  swap_declined           — your swap request was declined by the other employee
  swap_pending_approval   — (admin) both employees agreed, needs manager action
  swap_approved           — swap fully approved by manager
  swap_manager_declined   — swap declined by manager
  time_off_request        — (admin) employee submitted a time-off request
  time_off_approved       — your time-off request was approved
  time_off_declined       — your time-off request was declined

Use the "type" to deep-link into the appropriate screen when the user taps
a push notification:
  new_message   → navigate to Messages screen, open thread with thread_id
  post_liked    → navigate to Feed, highlight post with post_id
  post_comment  → navigate to Feed, open comments for post_id
  swap_*        → navigate to Schedule → Swaps tab
  time_off_*    → navigate to Schedule → Time Off tab

── Server setup for push notifications ──────────────────────────────────────────

Set these environment variables on the server:

  FIREBASE_PROJECT_ID=your-firebase-project-id
  FIREBASE_SERVICE_ACCOUNT_JSON=<contents of serviceAccountKey.json, single line>

Push is silently disabled when these vars are not set.

On the mobile client:
  1. Get an FCM/APNs device token from the platform SDK after login.
  2. POST /api/mobile/device-token with { token, platform: 'ios'|'android' }.
  3. On logout, DELETE /api/mobile/device-token to unregister.


────────────────────────────────────────────────────────────────────────────────
15. ERROR HANDLING — Standard error responses
────────────────────────────────────────────────────────────────────────────────

All endpoints use standard HTTP status codes.

Status  Meaning
──────  ────────────────────────────────────────────────────────────────────────
200     Success
400     Bad request — missing or invalid input
401     Unauthorized — missing, expired, or invalid Bearer token
403     Forbidden — authenticated but insufficient permissions
404     Not found — resource or org does not exist
409     Conflict — wrong state, duplicate, etc.
410     Gone — invite token already used or expired
500     Internal server error

Error response body always follows this format:
  { "error": "Human-readable description of what went wrong" }

TOKEN EXPIRY HANDLING:
  When you receive a 401, the token has expired (7-day lifetime).
  Re-authenticate by calling POST /api/mobile/auth again and save the new token.


────────────────────────────────────────────────────────────────────────────────
16. ENDPOINT QUICK REFERENCE
────────────────────────────────────────────────────────────────────────────────

METHOD  PATH                                      AUTH    DESCRIPTION
──────  ────────────────────────────────────────  ──────  ────────────────────────────────────────────
POST    /api/mobile/auth                          No      Login → get Bearer token
GET     /api/mobile/register?token=…              No      Validate invite token
POST    /api/mobile/register                      No      Complete registration
POST    /api/mobile/device-token                  Bearer  Register device for push notifications
DELETE  /api/mobile/device-token                  Bearer  Unregister device (logout)
GET     /api/mobile/notifications                 Bearer  List in-app notifications + unread count
PATCH   /api/mobile/notifications                 Bearer  Mark notification(s) as read
GET     /api/mobile/feed                          Bearer  Dashboard feed with likes/comments
POST    /api/mobile/feed                          Bearer  Create a post
POST    /api/mobile/feed/like?id=…                Bearer  Toggle like on a post (notifies author)
GET     /api/mobile/feed/comments?postId=…        Bearer  Load comments for a post
POST    /api/mobile/feed/comments?postId=…        Bearer  Add a comment (notifies author + engaged)
DELETE  /api/mobile/feed/comments?id=…            Bearer  Delete a comment
GET     /api/mobile/barred                        Bearer  Barred persons list
POST    /api/mobile/barred                        Bearer  Add a barred person (admin/permission)
GET     /api/mobile/out-of-stock                  Bearer  Low/out-of-stock inventory
GET     /api/mobile/schedule                      Bearer  Full org schedule with pending swaps
GET     /api/mobile/schedule/swap                 Bearer  List my shift swap requests
POST    /api/mobile/schedule/swap                 Bearer  Create a shift swap request
PATCH   /api/mobile/schedule/swap                 Bearer  Accept or decline a swap (target employee)
GET     /api/mobile/schedule/time-off             Bearer  List my time-off requests
POST    /api/mobile/schedule/time-off             Bearer  Submit a time-off request
PATCH   /api/mobile/schedule/time-off             Bearer  Approve or decline a request (admin only)
GET     /api/mobile/messages                      Bearer  List all threads (inbox) with unread counts
POST    /api/mobile/messages                      Bearer  Start a thread or send first message
GET     /api/mobile/messages/<threadId>           Bearer  Get paginated messages; marks thread read
POST    /api/mobile/messages/<threadId>           Bearer  Send a message to an existing thread
DELETE  /api/mobile/messages/<threadId>           Bearer  Leave a thread
GET     /api/mobile/messages/poll?since=<ISO>     Bearer  Poll for new messages across all threads
GET     /api/mobile/profile                       Bearer  Get your profile (no password fields)
PATCH   /api/mobile/profile                       Bearer  Update name, email, phone, bio, etc.
POST    /api/mobile/profile/picture               Bearer  Upload profile picture (base64 data-URI)
DELETE  /api/mobile/profile/picture               Bearer  Remove profile picture
GET     /api/mobile/settings                      Bearer  Get notification preferences + address
PATCH   /api/mobile/settings                      Bearer  Update notification preferences + address


────────────────────────────────────────────────────────────────────────────────
17. SECURITY NOTES
────────────────────────────────────────────────────────────────────────────────

- Tokens are signed JWT (HS256). Never decode and trust the payload client-side
  for security decisions — the server validates the signature on every request.

- Store tokens ONLY in secure storage (Keychain / EncryptedSharedPreferences).
  Never store in AsyncStorage — it is unencrypted.

- Tokens expire after 7 days. Handle 401 responses by clearing the stored token
  and redirecting the user to the login screen.

- Invite tokens are single-use. Once a registration completes, the token is
  marked used and cannot be reused.

- All returned data is scoped to the authenticated user's organization. The
  server enforces this — it is not possible to access another org's data with
  a valid token from a different org.

- HTTPS is required. All endpoints are served over TLS only.

- Passwords and PINs are NEVER returned by any API endpoint. They are stored
  as bcrypt hashes (password_hash, pin_hash). If you observe a raw password
  in any response, treat it as a security incident.

- Image uploads (profile picture, message images) are validated server-side:
  must start with "data:image/", must be within size limits. Never trust the
  content-type from the client alone.

- Thread membership is enforced on every message read/send. You cannot read
  or send to a thread you are not a member of, even if you know the thread ID.

- All user lookups for messaging validate that target users belong to the same
  organization before creating threads. Cross-org messaging is impossible.

- The poll endpoint (/messages/poll) uses the server's current timestamp in
  its response ("server_time"). Always advance your "since" cursor to this
  value, not to client clock, to avoid clock-skew gaps or double-delivers.

================================================================================
  TopShelf Inventory — Mobile API Reference
  Server: https://www.topshelfInventory.com
  API Version: 3.0  |  Last updated: 2026-05-05
================================================================================


================================================================================
  TOPSHELF INVENTORY — DEVELOPER API REFERENCE (v1)
  Base URL: https://www.topshelfInventory.com/api/v1
  For custom integrations, POS systems, and third-party software.
================================================================================


────────────────────────────────────────────────────────────────────────────────
OVERVIEW
────────────────────────────────────────────────────────────────────────────────

The Developer API is a separate, versioned API from the Mobile API. It uses
static API keys instead of JWT tokens and is intended for server-to-server
integrations, POS systems, custom dashboards, and automation scripts.

Key differences from the Mobile API:
  - Auth:        Bearer API key (tsk_live_…) instead of JWT token
  - Base path:   /api/v1/  instead of /api/mobile/
  - Scopes:      Keys are created with specific scopes limiting what they can do
  - Response:    Standard envelope { data, meta } on every response
  - Audience:    Server-side integrations, not end-user mobile apps

API keys are created by admins at: Admin → Settings → Developer API
Keys are scoped to a single organization. Each key grants only the scopes
selected at creation time.

Authentication:
  All requests must include the Authorization header:
    Authorization: Bearer tsk_live_<your-key>

Every request body must use JSON with the header:
  Content-Type: application/json

All data is scoped to the organization that owns the API key.


────────────────────────────────────────────────────────────────────────────────
STANDARD RESPONSE ENVELOPE
────────────────────────────────────────────────────────────────────────────────

Every v1 response wraps data in a consistent envelope:

Success (single resource):
{
  "data": { ...resource fields },
  "meta": {
    "api_version": "v1",
    "request_id": "a3f8b2c1d4e5f6a7",
    "timestamp": "2026-05-09T14:00:00.000Z"
  }
}

Success (list):
{
  "data": [ ...array of resources ],
  "meta": {
    "api_version": "v1",
    "request_id": "a3f8b2c1d4e5f6a7",
    "timestamp": "2026-05-09T14:00:00.000Z",
    "total": 142,
    "limit": 100,
    "offset": 0
  }
}

Error:
{
  "error": {
    "code": "NOT_FOUND",
    "message": "Item not found."
  },
  "meta": {
    "api_version": "v1",
    "request_id": "a3f8b2c1d4e5f6a7",
    "timestamp": "2026-05-09T14:00:00.000Z"
  }
}

Error codes:
  UNAUTHORIZED   — missing or invalid API key (HTTP 401)
  FORBIDDEN      — key lacks the required scope (HTTP 403)
  BAD_REQUEST    — missing or invalid parameters (HTTP 400)
  NOT_FOUND      — resource does not exist (HTTP 404)
  INTERNAL_ERROR — server-side error (HTTP 500)


────────────────────────────────────────────────────────────────────────────────
SCOPES
────────────────────────────────────────────────────────────────────────────────

Each API key is created with one or more scopes. Calling an endpoint without
the required scope returns a 403 FORBIDDEN error.

  *                 Full access to all endpoints
  inventory:read    List items, get item, low-stock report
  inventory:write   Adjust stock, transfer, create/update/delete items
  orders:read       List and view purchase orders
  orders:write      Create, update, and delete purchase orders
  audits:read       View audit history sessions
  audits:write      Submit inventory audits (set actual quantities)
  locations:read    List locations
  categories:read   List categories


────────────────────────────────────────────────────────────────────────────────
18. INVENTORY — Stock items and quantities
────────────────────────────────────────────────────────────────────────────────

── List inventory ────────────────────────────────────────────────────────────────

GET /api/v1/inventory
Required scope: inventory:read

QUERY PARAMETERS (all optional):
  ?location_id=<id>    Scope quantities to a specific location
  ?category=Spirits    Filter by category name
  ?low_stock=true      Only return items at or below their threshold
  ?limit=100           Page size (default 100, max 500)
  ?offset=0            Page offset (default 0)
  ?sort=name           "name" | "quantity" | "type" (default: name)

RESPONSE (200 OK):
{
  "data": [
    {
      "id": 1,
      "name": "Vodka",
      "category": "Spirits",
      "sub_category": "Vodka",
      "supplier": "Southern Glazers",
      "unit_cost": 22.50,
      "sale_price": null,
      "order_size": [{ "label": "Case", "amount": 12 }],
      "low_stock_threshold": 10,
      "quantity": 24.5
    }
  ],
  "meta": { "total": 142, "limit": 100, "offset": 0, ... }
}

── Create an item ────────────────────────────────────────────────────────────────

POST /api/v1/inventory
Required scope: inventory:write

Creates a new inventory item. Optionally sets an initial stock quantity at
a specific location.

REQUEST BODY:
{
  "name": "Tito's Vodka 750ml",     ← required; must be unique within org
  "category": "Spirits",            ← optional — primary category
  "sub_category": "Vodka",          ← optional — secondary category
  "supplier": "Southern Glazers",   ← optional
  "unit_cost": 22.50,               ← optional — cost per unit
  "sale_price": 8.00,               ← optional — sale price per unit
  "order_size": 12,                 ← optional — units per order
  "low_stock_threshold": 10,        ← optional (default: 5)
  "initial_quantity": 24,           ← optional — starting stock quantity
  "location_id": 1                  ← optional — where to set initial_quantity
}

RESPONSE (201 Created):
  { "data": { "id": 88, "created": true }, "meta": { ... } }

ERROR RESPONSES:
  400 — name is required
  400 — an item with that name already exists

── Get a single item ────────────────────────────────────────────────────────────

GET /api/v1/inventory/{itemId}
Required scope: inventory:read

RESPONSE (200 OK):
{
  "data": {
    "id": 1,
    "name": "Vodka",
    "category": "Spirits",
    "sub_category": "Vodka",
    "supplier": "Southern Glazers",
    "unit_cost": 22.50,
    "sale_price": null,
    "order_size": [{ "label": "Case", "amount": 12 }],
    "low_stock_threshold": 10,
    "barcodes": [],
    "aliases": [],
    "total_quantity": 48,
    "quantities": [
      { "location_id": 1, "location_name": "Main Bar", "quantity": 24 },
      { "location_id": 2, "location_name": "Back Bar", "quantity": 24 }
    ]
  },
  "meta": { ... }
}

── Update an item ────────────────────────────────────────────────────────────────

PUT /api/v1/inventory/{itemId}
Required scope: inventory:write

Send only the fields you want to change. All fields optional.

REQUEST BODY:
{
  "name": "Premium Vodka",
  "category": "Spirits",
  "sub_category": "Vodka",
  "supplier": "Distributor Co",
  "unit_cost": 22.50,
  "sale_price": 8.00,
  "order_size": 12,
  "low_stock_threshold": 12
}

RESPONSE (200 OK):
  { "data": { "id": 1, "updated": true }, "meta": { ... } }

── Delete an item ────────────────────────────────────────────────────────────────

DELETE /api/v1/inventory/{itemId}
Required scope: inventory:write

Permanently deletes the item and all inventory quantity records.

RESPONSE (200 OK):
  { "data": { "id": 1, "deleted": true }, "meta": { ... } }

── Add stock (restock) ───────────────────────────────────────────────────────────

POST /api/v1/inventory/{itemId}/restock
Required scope: inventory:write

Add stock for a specific item at a location. Use this when receiving a delivery,
transferring in product, or correcting a count upward.
Response includes quantity_before and quantity_after for confirmation.

REQUEST BODY:
{
  "quantity": 12,           ← required — amount to add (must be positive)
  "location_id": 1,         ← optional (defaults to org's first location)
  "note": "Delivery from Southern Glazers"   ← optional, logged in activity
}

RESPONSE (200 OK):
{
  "data": {
    "item_id": 1,
    "item_name": "Vodka",
    "location_id": 1,
    "quantity_added": 12,
    "quantity_before": 10,
    "quantity_after": 22
  },
  "meta": { ... }
}

ERROR RESPONSES:
  400 — quantity must be a positive number
  404 — item not found in this organization
  404 — location not found in this organization

── Remove stock (consume) ────────────────────────────────────────────────────────

POST /api/v1/inventory/{itemId}/consume
Required scope: inventory:write

Remove stock for a specific item at a location. Use this when recording usage,
breakage, or any stock reduction. Returns a 400 if the location has insufficient
stock — quantity will never go negative.

REQUEST BODY:
{
  "quantity": 2,            ← required — amount to remove (must be positive)
  "location_id": 1,         ← optional (defaults to org's first location)
  "note": "2 bottles broken during service"   ← optional, logged in activity
}

RESPONSE (200 OK):
{
  "data": {
    "item_id": 1,
    "item_name": "Vodka",
    "location_id": 1,
    "quantity_removed": 2,
    "quantity_before": 22,
    "quantity_after": 20
  },
  "meta": { ... }
}

ERROR RESPONSES:
  400 — quantity must be a positive number
  400 — insufficient stock (shows available vs. requested)
  404 — item not found in this organization
  404 — location not found in this organization

── Adjust stock (signed delta) ───────────────────────────────────────────────────

POST /api/v1/inventory/adjust
Required scope: inventory:write

General-purpose signed adjustment. Positive = add, negative = remove.
Prefer /restock and /consume for clearer intent; use /adjust for programmatic
integrations where the sign is computed dynamically.
Quantity is floored at 0 (will not go negative).

REQUEST BODY:
{
  "item_id": 1,         ← required
  "change": -2,         ← required, non-zero; negative = subtract
  "location_id": 1,     ← optional (defaults to org's first location)
  "note": "Variance correction"   ← optional
}

RESPONSE (200 OK):
{
  "data": {
    "item_id": 1,
    "item_name": "Vodka",
    "location_id": 1,
    "change": -2,
    "quantity_after": 20
  },
  "meta": { ... }
}

ERROR RESPONSES:
  400 — change is zero or missing
  404 — item not found
  404 — location not found

── Transfer stock between locations ──────────────────────────────────────────────

POST /api/v1/inventory/transfer
Required scope: inventory:write

Move stock for an item from one location to another. Fails if the source
location has insufficient stock.

REQUEST BODY:
{
  "item_id": 1,             ← required
  "from_location_id": 1,   ← required
  "to_location_id": 2,     ← required (must differ from from_location_id)
  "quantity": 6,            ← required, must be positive
  "note": "Restock upstairs bar"   ← optional
}

RESPONSE (200 OK):
{
  "data": {
    "item_id": 1,
    "item_name": "Vodka",
    "from_location_id": 1,
    "to_location_id": 2,
    "quantity_transferred": 6
  },
  "meta": { ... }
}

ERROR RESPONSES:
  400 — insufficient stock at source location (shows available vs. requested)
  400 — from and to location are the same
  404 — item, source location, or destination location not found

── Low-stock report ──────────────────────────────────────────────────────────────

GET /api/v1/inventory/low-stock
Required scope: inventory:read

Returns items at or below their low_stock_threshold. Each item includes a
"deficit" field showing how many units short of the threshold it is.

QUERY PARAMETERS (all optional):
  ?location_id=<id>    Scope to a specific location
  ?limit=100           Page size (default 100, max 500)
  ?offset=0            Page offset

RESPONSE (200 OK):
{
  "data": [
    {
      "id": 5,
      "name": "Gin",
      "category": "Spirits",
      "supplier": "Southern Glazers",
      "quantity": 2,
      "low_stock_threshold": 10,
      "deficit": 8
    }
  ],
  "meta": { "total": 12, "limit": 100, "offset": 0, ... }
}

NOTES:
- Items with include_in_low_stock_alerts = false are excluded.
- Results sorted by quantity ascending (most urgent first).
- deficit = max(0, low_stock_threshold - quantity).


────────────────────────────────────────────────────────────────────────────────
19. LOCATIONS
────────────────────────────────────────────────────────────────────────────────

GET /api/v1/locations
Required scope: locations:read

Returns all locations configured for the organization.

RESPONSE (200 OK):
{
  "data": [
    {
      "id": 1,
      "name": "Main Bar",
      "address": "123 Main Street",
      "created_at": "2025-09-01T00:00:00Z"
    }
  ],
  "meta": { "total": 2, "limit": 2, "offset": 0, ... }
}


────────────────────────────────────────────────────────────────────────────────
20. CATEGORIES
────────────────────────────────────────────────────────────────────────────────

GET /api/v1/categories
Required scope: categories:read

QUERY PARAMETERS (all optional):
  ?include_counts=true    Include item count per category

RESPONSE (200 OK):
{
  "data": [
    {
      "id": 1,
      "name": "Spirits",
      "description": null,
      "item_count": 42        ← only included when ?include_counts=true
    }
  ],
  "meta": { "total": 8, ... }
}


────────────────────────────────────────────────────────────────────────────────
21. ORDERS — Purchase orders
────────────────────────────────────────────────────────────────────────────────

── List orders ──────────────────────────────────────────────────────────────────

GET /api/v1/orders
Required scope: orders:read

QUERY PARAMETERS (all optional):
  ?status=PENDING          Filter: "PENDING" | "DELIVERED" | "CANCELLED" | "all"
  ?limit=50                Page size (default 50, max 200)
  ?offset=0                Page offset

RESPONSE (200 OK):
{
  "data": [
    {
      "id": 1,
      "status": "PENDING",
      "location_id": 1,
      "location_name": "Main Bar",
      "item_count": 4,
      "expected_delivery": "2026-05-15",
      "notes": "Weekly restock",
      "created_at": "2026-05-09T10:00:00Z",
      "updated_at": "2026-05-09T10:00:00Z"
    }
  ],
  "meta": { "total": 24, "limit": 50, "offset": 0, ... }
}

── Create an order ───────────────────────────────────────────────────────────────

POST /api/v1/orders
Required scope: orders:write

REQUEST BODY:
{
  "location_id": 1,                  ← required
  "expected_delivery": "2026-05-15", ← optional, ISO date
  "notes": "Weekly restock",         ← optional
  "items": [                         ← required, non-empty array
    { "item_id": 1, "quantity": 12, "unit_cost": 22.50 },
    { "item_id": 5, "quantity": 6 }
  ]
}

"items" fields:
  item_id    integer  required
  quantity   number   required
  unit_cost  number   optional

RESPONSE (201 Created):
{
  "data": {
    "id": 42,
    "status": "PENDING",
    "created_at": "2026-05-09T10:00:00Z",
    "items": [
      { "id": 101, "item_id": 1, "item_name": "Vodka", "quantity": 12 },
      { "id": 102, "item_id": 5, "item_name": "Gin", "quantity": 6 }
    ]
  },
  "meta": { ... }
}

ERROR RESPONSES:
  404 — location_id not found in this organization
  400 — items array is empty
  400 — unknown item_id (skipped silently — only valid items are inserted)

── Get an order ─────────────────────────────────────────────────────────────────

GET /api/v1/orders/{id}
Required scope: orders:read

PATH PARAMETER:
  id  integer  required — order ID

RESPONSE (200 OK):
{
  "data": {
    "id": 42,
    "status": "PENDING",
    "location_id": 1,
    "location_name": "Main Bar",
    "expected_delivery": "2026-05-15",
    "notes": "Weekly restock",
    "created_at": "2026-05-09T10:00:00Z",
    "updated_at": "2026-05-09T10:00:00Z",
    "items": [
      {
        "id": 101,
        "item_id": 1,
        "item_name": "Vodka",
        "quantity": 12,
        "quantity_received": null,
        "unit_cost": 22.50
      }
    ]
  },
  "meta": { ... }
}

── Update an order ───────────────────────────────────────────────────────────────

PUT /api/v1/orders/{id}
Required scope: orders:write

Updates status and/or notes. Send only the fields to change.

REQUEST BODY:
{
  "status": "DELIVERED",              ← optional
  "notes": "All items received"       ← optional
}

Valid statuses: PENDING | DELIVERED | CANCELLED | IN_TRANSIT | PARTIALLY_RECEIVED

RESPONSE (200 OK):
  { "data": { "id": 42, "updated": true }, "meta": { ... } }

── Delete an order ───────────────────────────────────────────────────────────────

DELETE /api/v1/orders/{id}
Required scope: orders:write

Only PENDING orders can be deleted. Delivered or cancelled orders cannot be
deleted — update their status instead.

RESPONSE (200 OK):
  { "data": { "id": 42, "deleted": true }, "meta": { ... } }

ERROR RESPONSES:
  404 — order not found
  400 — order is not in PENDING status


────────────────────────────────────────────────────────────────────────────────
22. AUDITS — Inventory count sessions
────────────────────────────────────────────────────────────────────────────────

── List audit history ────────────────────────────────────────────────────────────

GET /api/v1/audits
Required scope: audits:read

Returns audit sessions grouped by auditor and timestamp (minute granularity).

QUERY PARAMETERS (all optional):
  ?limit=50     Page size (default 50, max 200)
  ?offset=0     Page offset

RESPONSE (200 OK):
{
  "data": [
    {
      "session_id": 8801,
      "auditor_name": "Jane Smith",
      "audited_at": "2026-05-09T14:00:00Z",
      "item_count": 18
    }
  ],
  "meta": { "total": 42, "limit": 50, "offset": 0, ... }
}

NOTE: "auditor_name" is null for audits submitted via API (no user context).
      "session_id" is the minimum activity_log ID in that session — use it
      for display/reference purposes only.

── Submit an audit ───────────────────────────────────────────────────────────────

POST /api/v1/audits
Required scope: audits:write

Set the actual (counted) quantities for items at a location. This OVERWRITES
current quantities with the actual values counted. Variance (actual - previous)
is calculated automatically and logged.

REQUEST BODY:
{
  "location_id": 1,           ← required
  "label": "Weekly count",    ← optional, recorded in the activity log
  "items": [                  ← required, non-empty array
    { "item_id": 1, "actual_quantity": 22, "note": "2 broken" },
    { "item_id": 5, "actual_quantity": 8 }
  ]
}

"items" fields:
  item_id          integer  required
  actual_quantity  number   required — the true counted quantity (can be 0)
  note             string   optional — recorded alongside this item's audit entry

RESPONSE (201 Created):
{
  "data": {
    "location_id": 1,
    "label": "Weekly count",
    "items_audited": 2,
    "items": [
      {
        "item_id": 1,
        "item_name": "Vodka",
        "previous_quantity": 24,
        "actual_quantity": 22,
        "variance": -2
      },
      {
        "item_id": 5,
        "item_name": "Gin",
        "previous_quantity": 6,
        "actual_quantity": 8,
        "variance": 2
      }
    ]
  },
  "meta": { ... }
}

ERROR RESPONSES:
  404 — location_id not found
  400 — items array is empty

NOTE: Items not found in this organization are silently skipped.
      All changes are applied atomically in a single transaction.
      Activity logs are written with source: "api" and the key name.


────────────────────────────────────────────────────────────────────────────────
23. DEVELOPER API — ENDPOINT QUICK REFERENCE
────────────────────────────────────────────────────────────────────────────────

METHOD  PATH                                    SCOPE              DESCRIPTION
──────  ──────────────────────────────────────  ─────────────────  ────────────────────────────────────────────
GET     /api/v1/inventory                       inventory:read     List all items with quantities
POST    /api/v1/inventory                       inventory:write    Create a new inventory item
GET     /api/v1/inventory/{itemId}              inventory:read     Get single item + per-location quantities
PUT     /api/v1/inventory/{itemId}              inventory:write    Update item metadata
DELETE  /api/v1/inventory/{itemId}              inventory:write    Delete item and all inventory records
POST    /api/v1/inventory/{itemId}/restock      inventory:write    Add stock (delivery, receiving)
POST    /api/v1/inventory/{itemId}/consume      inventory:write    Remove stock (usage, breakage)
POST    /api/v1/inventory/adjust                inventory:write    Signed delta adjustment (+/-)
POST    /api/v1/inventory/transfer              inventory:write    Transfer stock between locations
GET     /api/v1/inventory/low-stock             inventory:read     Items at or below their threshold
GET     /api/v1/locations                       locations:read     List all locations
GET     /api/v1/categories                      categories:read    List all item categories
GET     /api/v1/orders                          orders:read        List purchase orders
POST    /api/v1/orders                          orders:write       Create a purchase order
GET     /api/v1/orders/{id}                     orders:read        Get order with line items
PUT     /api/v1/orders/{id}                     orders:write       Update order status or notes
DELETE  /api/v1/orders/{id}                     orders:write       Delete a PENDING order
GET     /api/v1/audits                          audits:read        List audit history sessions
POST    /api/v1/audits                          audits:write       Submit a counted audit
GET     /api/v1/recipes                         recipes:read       List recipes (local and/or global)
POST    /api/v1/recipes                         recipes:write      Create an org-local recipe
GET     /api/v1/recipes/{id}                    recipes:read       Get a single recipe
PUT     /api/v1/recipes/{id}                    recipes:write      Update an org-local recipe
DELETE  /api/v1/recipes/{id}                    recipes:write      Delete an org-local recipe
POST    /api/v1/recipes/{id}/image              recipes:write      Upload or replace recipe image
DELETE  /api/v1/recipes/{id}/image              recipes:write      Remove recipe image


────────────────────────────────────────────────────────────────────────────────
25. DEVELOPER API — DRINK RECIPES
────────────────────────────────────────────────────────────────────────────────

TopShelf supports two recipe libraries:
  • Org-local  — recipes your admin creates, readable and writable via the API
  • Global     — curated recipes managed by TopShelf staff, read-only via the API

Which libraries are searched is configured per-org in General Settings
(recipe_search_source: "local" | "global" | "both"). The ?source query param
overrides this per request.

RECIPE ID FORMAT
  l:<id>   org-local recipe (e.g. "l:3")
  g:<id>   global recipe    (e.g. "g:12")
  <int>    plain integer — resolves local first, then global

Only local recipes (l: prefix or plain integer) can be created, updated,
deleted, or have images uploaded via the API. Global recipes are read-only.

────────────────────────────────────────────────────────────────────────────────
GET /api/v1/recipes
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:read

List drink recipes according to the org's recipe_search_source setting.
Results are merged with the primary library first, deduplicating by name.

QUERY PARAMS:
  q          string   Full-text search across name and description
  category   string   Filter by category ("Cocktail", "Shot", "Mocktail", etc.)
  source     string   "local" | "global" | "both" — overrides org setting
  limit      integer  Page size (default 50, max 200)
  offset     integer  Page offset (default 0)

RESPONSE 200:
{
  "data": [
    {
      "id": "l:3",
      "name": "Margarita",
      "source": "local",
      "description": "Classic tequila cocktail",
      "category": "Cocktail",
      "tags": ["citrus", "popular"],
      "ingredients": [
        { "amount": "1.5", "unit": "oz", "item": "Tequila" },
        { "amount": "0.75", "unit": "oz", "item": "Triple Sec" },
        { "amount": "1", "unit": "oz", "item": "Lime Juice" }
      ],
      "instructions": "Shake with ice and strain into a salted rim glass.",
      "image_url": "/uploads/3f7a1b2c.jpg",
      "is_active": true,
      "created_at": "2026-05-01T10:00:00Z",
      "updated_at": "2026-05-10T14:30:00Z"
    }
  ],
  "meta": { "total": 24, "limit": 50, "offset": 0 }
}

────────────────────────────────────────────────────────────────────────────────
POST /api/v1/recipes
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:write
Content-Type: application/json

Create a new org-local recipe.

REQUEST BODY:
{
  "name": "Classic Mojito",          // required
  "description": "...",              // optional
  "category": "Cocktail",            // optional — Cocktail|Shot|Mocktail|Beer|Wine|Spirit|Other
  "tags": ["mint", "rum"],           // optional — string array
  "ingredients": [                   // optional
    { "amount": "2", "unit": "oz", "item": "White Rum" },
    { "amount": "0.75", "unit": "oz", "item": "Lime Juice" },
    { "amount": "10", "item": "Mint Leaves" }
  ],
  "instructions": "Muddle mint..."   // optional
}

RESPONSE 201:
{
  "data": {
    "id": "l:7",
    "name": "Classic Mojito",
    "source": "local"
  }
}

────────────────────────────────────────────────────────────────────────────────
GET /api/v1/recipes/{id}
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:read

Get a single recipe by ID. Accepts l:<id>, g:<id>, or plain integer.

RESPONSE 200: Full recipe object (same shape as list item above)
RESPONSE 404: Recipe not found

────────────────────────────────────────────────────────────────────────────────
PUT /api/v1/recipes/{id}
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:write
Content-Type: application/json

Update an org-local recipe. All fields are optional — send only the fields
you want to change. Global recipes (g: prefix) return 400.

REQUEST BODY (all fields optional):
{
  "name": "Spicy Margarita",
  "description": "With jalapeño",
  "category": "Cocktail",
  "tags": ["citrus", "spicy"],
  "ingredients": [...],
  "instructions": "..."
}

RESPONSE 200:
{ "data": { "id": "l:3", "updated": true } }

RESPONSE 400: Cannot update global recipes via the API
RESPONSE 404: Recipe not found or not owned by this org

────────────────────────────────────────────────────────────────────────────────
DELETE /api/v1/recipes/{id}
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:write

Delete an org-local recipe. Global recipes (g: prefix) return 400.

RESPONSE 200: { "data": { "id": "l:3", "deleted": true } }
RESPONSE 400: Cannot delete global recipes via the API
RESPONSE 404: Recipe not found or not owned by this org

────────────────────────────────────────────────────────────────────────────────
POST /api/v1/recipes/{id}/image
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:write
Content-Type: multipart/form-data

Upload or replace the image for an org-local recipe. Only local recipes
are supported (l:<id> or plain integer). Global recipes return 400.

FORM FIELDS:
  image   File   Required. jpeg, png, gif, or webp — max 5 MB.

RESPONSE 200:
{ "ok": true, "image_url": "/uploads/3f7a1b2c.jpg" }

RESPONSE 400: No file provided | invalid MIME type | file too large | global recipe ID
RESPONSE 404: Recipe not found or not owned by this org

────────────────────────────────────────────────────────────────────────────────
DELETE /api/v1/recipes/{id}/image
────────────────────────────────────────────────────────────────────────────────
Scope: recipes:write

Remove the image from an org-local recipe (sets image_url to null).

RESPONSE 200: { "ok": true }
RESPONSE 400: Global recipe ID not supported

────────────────────────────────────────────────────────────────────────────────
RECIPE SCOPES SUMMARY
────────────────────────────────────────────────────────────────────────────────
  recipes:read   GET /api/v1/recipes, GET /api/v1/recipes/{id}
  recipes:write  POST/PUT/DELETE /api/v1/recipes, POST/DELETE /api/v1/recipes/{id}/image
  *              All of the above plus all other scopes


────────────────────────────────────────────────────────────────────────────────
24. DEVELOPER API — SECURITY NOTES
────────────────────────────────────────────────────────────────────────────────

- API keys start with tsk_live_. Store them in environment variables or a
  secrets manager — never hardcode them in source code or commit them.

- Keys are shown in full only once at creation. After that, only the prefix
  (first 8 characters) is visible in the admin UI. If a key is lost, revoke
  it and create a new one.

- Revoke unused keys immediately. Each key has a name and last-used timestamp
  visible in the admin UI to help identify stale credentials.

- Assign the minimum scopes needed. A key for a POS system reading stock
  levels only needs inventory:read — not inventory:write or orders:write.

- All API activity is logged in activity_logs with source: "api" and the
  key name. Audit trails are preserved for compliance.

- Keys are scoped to one organization. A key from Org A cannot access Org B's
  data under any circumstances.

- HTTPS is required. All endpoints are served over TLS only.

================================================================================
  TopShelf Inventory — Developer API Reference (v1)
  Server: https://www.topshelfInventory.com/api/v1
  Docs:   https://www.topshelfInventory.com/admin/developer
  API Version: v1  |  Last updated: 2026-05-10
================================================================================
